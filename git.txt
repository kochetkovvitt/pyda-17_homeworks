git config --global user.name "Kochetkov Vitaliy"
git config --global user.email "kochetkov.vitt@gmail.com"
git config --global core.editor nano
----------------------------------
git init
----------------------------------
git add *
git add *.py
git add git.txt
----------------------------------
git status
----------------------------------
git commit -a -m "message"

# флаг -a ( --all ) говорит о том, что мы добавляем в stage все удалённые/изменённые файлы (но не новые, новые нужно добавлять отдельно).
# флаг -m "сообщение коммита" ( --message="сообщение коммита" ) позволяет не открывать редактор, а указывать сообщение прямо в командной строке.
----------------------------------
git log
# посмотреть историю коммитов можно с помощью команды git log
----------------------------------
git show 5e0c1
# посмотреть полную информацию о коммите - git show <id-коммитта>
----------------------------------
git commit --amend -m "new message" 
# исправить комментарий последнего коммита
----------------------------------
git revert <commit-id>
# «зеркальный» коммит, который отменит действие предыдущего
----------------------------------
git rm --cached test.txt
# удалить файл из stage area
----------------------------------
git – краткая справка по git;
git help <command> – справка по конкретной команде git.
----------------------------------
https://github.com/kochetkovvitt/pyda-17_homeworks.git
git remote add origin https://github.com/kochetkovvitt/pyda-17_homeworks.git
git branch -M main
git push -u origin main
----------------------------------








